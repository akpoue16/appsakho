<form style="display:inline !important" method="post" action="{{ path('app_tribunal_delete', {'id': tribunal.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ce tribunal: {{ tribunal.titre }}?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tribunal.id) }}">
    <button class="btn btn-danger" style="padding: 0px 2px !important"><i class="fas fa-trash-alt fa-sm text-white-10"></i></button>
</form>
