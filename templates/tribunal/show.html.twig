{% extends 'base.html.twig' %}

{% block title %}Tribunal{% endblock %}

{% block body %}
  <!-- Page Heading -->
    <div class="d-sm-flex align-items-center justify-content-between mb-4">
        <h1 class="h3 mb-0 text-gray-800">Tribunal: {{ tribunal.titre }}</h1>
    </div>
    <!-- DataTales Example -->
    <div class="card shadow mb-4">
                        {# <div class="card-header py-3">
                            <h6 class="m-0 font-weight-bold text-primary">DataTables Example</h6>
                        </div> #}
        <div class="card-body">

    <div class="row">
        <div class="col-md-12">

    <table class="table">
    
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tribunal.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ tribunal.titre }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ tribunal.commentaire }}</td>
            </tr>
        </tbody>
    </table>
   </div>
    </div>
     <div class="row">
        <div class="col">
                <a class="btn btn-primary" href="{{ path('app_tribunal_index') }}">
                    <i class="fas fa-arrow-left fa-sm text-white-10"></i> Retour à la liste
                </a>
                <a class="btn btn-success" href="{{ path('app_tribunal_edit', {'id': tribunal.id}) }}">
                    <i class="fas fa-pencil-alt fa-sm text-white-10"> </i> Modifier
                </a>
        </div>
    </div>

        </div>
    </div>
{% endblock %}
