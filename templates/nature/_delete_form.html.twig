<form method="post" action="{{ path('app_nature_delete', {'id': nature.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nature.id) }}">
    <button class="btn">Delete</button>
</form>
