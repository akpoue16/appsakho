{% extends 'base.html.twig' %}

{% block title %}Qualite{% endblock %}

{% block body %}
    <h1>Qualite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ qualite.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ qualite.titre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_qualite_index') }}">back to list</a>

    <a href="{{ path('app_qualite_edit', {'id': qualite.id}) }}">edit</a>

    {{ include('qualite/_delete_form.html.twig') }}
{% endblock %}
