
{{ form_start(form) }}
   <div class="row">
        <div class="col">  
            <div class="form-row">
                <div class="col-2">
                    {{ form_row(form.titre,{'label': 'Titre', 'attr':{'placeholder':'titre','class':'form-control'}}) }}
                </div>
            </div>
          
        </div>
        <div class="col-3">
            <div class="row mb-3">
                <div class="col">
                   
                </div>
            </div>
        </div>
   </div>
    <div class="row">
        <div class="col">
            <a href="{{ path('app_qualite_index') }}" class="btn btn-primary btn-icon-split">
                <span class="icon text-white-10">
                    <i class="fas fa-arrow-left fa-sm text-white-10"></i>
                </span>
                <span class="text">Retour à la liste</span>
            </a>
            <button class="btn btn-success btn-icon-split">
                <span class="icon text-white-10">
                    <i class="fas fa-save fa-sm text-white-10"></i>
                </span>
                <span class="text">{{ button_label|default('Enregistrer') }}</span>
            </button>
        </div>
    </div>
{{ form_end(form) }}