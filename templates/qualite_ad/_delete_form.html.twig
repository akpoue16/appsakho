<form method="post" action="{{ path('app_qualite_ad_delete', {'id': qualite_ad.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ qualite_ad.id) }}">
    <button class="btn">Delete</button>
</form>
