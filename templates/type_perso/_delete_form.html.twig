<form method="post" action="{{ path('app_typeperso_delete', {'id': type_perso.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_perso.id) }}">
	<button class="btn">Delete</button>
</form>
