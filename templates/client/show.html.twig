{% extends 'base.html.twig' %}

{% block title %}Client{% endblock %}

{% block body %}
     <!-- Page Heading -->
    <div class="d-sm-flex align-items-center justify-content-between mb-4">
        <h1 class="h3 mb-0 text-gray-800">Client: {{ client.code }}</h1>
    </div>
    <!-- DataTales Example -->
                    <div class="card shadow mb-4">
                        {# <div class="card-header py-3">
                            <h6 class="m-0 font-weight-bold text-primary">DataTables Example</h6>
                        </div> #}
                        <div class="card-body">

    <div class="row">
        <div class="col-md-12">
                <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ client.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ client.code }}</td>
            </tr>
            <tr>
                <th>RaisonSocial</th>
                <td>{{ client.raisonSocial }}</td>
            </tr>
            <tr>
                <th>Contact</th>
                <td>{{ client.contact }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ client.titre }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ client.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ client.prenom }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ client.tel }}</td>
            </tr>
            <tr>
                <th>Cel</th>
                <td>{{ client.cel }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ client.email }}</td>
            </tr>
            <tr>
                <th>Fax</th>
                <td>{{ client.fax }}</td>
            </tr>
        </tbody>
    </table> 
        
        </div>
    </div>

    <div class="row">
    <div class="col">
            <a class="btn btn-primary" href="{{ path('app_client_index') }}">
                <i class="fas fa-arrow-left fa-sm text-white-10"></i> Retour à la liste
            </a>
            <a class="btn btn-success" href="{{ path('app_client_edit', {'id': client.id}) }}">
            <i class="fas fa-pencil-alt fa-sm text-white-10"> </i> Modifier
            </a>
        </div>
    </div>

        </div>
    </div>
{% endblock %}
